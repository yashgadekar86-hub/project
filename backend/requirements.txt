# ===== Core Web =====
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2
python-multipart>=0.0.9

# ===== Database =====
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29
alembic>=1.13
psycopg2-binary>=2.9  # for alembic migrations
redis>=5.0

# ===== Auth / Security =====
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.0
cryptography>=42.0

# ===== Tasks =====
celery[redis]>=5.3

# ===== HTTP =====
httpx>=0.27

# ===== Data / ML / Quant =====
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
ta>=0.11  # Technical Analysis library
# Optional ML (lightweight baseline; heavy libs are optional extras)
xgboost>=2.0 ; platform_system != "Darwin" or platform_machine != "arm64"
joblib>=1.3

# ===== MetaTrader 5 =====
# Note: MetaTrader5 wheel is Windows-only; on Linux use it via Wine or keep
# the connector isolated. We gracefully degrade if the package is absent.
MetaTrader5>=5.0.45 ; platform_system == "Windows"

# ===== WebSocket =====
websockets>=12.0

# ===== Logging / Observability =====
python-json-logger>=2.0
structlog>=24.0

# ===== Testing =====
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
httpx>=0.27  # already listed
faker>=24.0
freezegun>=1.4

# ===== Utilities =====
python-dotenv>=1.0
pytz>=2024.1
tenacity>=8.2
email-validator>=2.1
psutil>=5.9
